<div class="container">
  <h1 class= "text-center font-weigth-bold py-5"><%= @client.name %></h1>
  <div class="text-center font-weigth-bold">
    <p>Email : <%= @client.email %></p>
    <p>Teléfono : <%= @client.phone%></p>
    <p>Estado : <%= @client.state%></p>
  </div>
 
  <div class="text-center">
    <%= link_to "Volver", clients_path, class: "btn btn-primary"%>
    <%= link_to "Editar", edit_client_path(@client), class: "btn btn-warning" %>
    <%= link_to "Eliminar", client_path(@client), method: :delete, data: { confirm: "Estas Seguro?" },  class: "btn btn-danger" %>
  </div>
  </div>